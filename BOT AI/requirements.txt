# Claude AI Chatbot Requirements

# ── Core Database ──────────────────────────────────────────────────────────────
pymongo>=4.6.0
dnspython>=2.6.0

# ── Security (REQUIRED) ────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # Load secrets from .env instead of hardcoding them
google-auth>=2.0.0          # Verify Google OAuth JWT tokens server-side
requests>=2.28.0            # Required by google-auth transport
cryptography>=41.0.0        # Cryptographic primitives (token signing etc.)

# ── Built-in Python modules (no install needed) ────────────────────────────────
# http.server, socketserver, urllib.request, json
# hashlib, secrets, datetime, collections, time, os

# ── Optional performance ───────────────────────────────────────────────────────
# ujson>=5.8.0              # Faster JSON parsing (drop-in replacement)